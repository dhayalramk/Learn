# Data Structure

## Lesson 0.1  - What is Data?
Data is information the computer stores and works with

-> Data 
-> Information 
-> Computer stores it 
-> Computer does something with it

----

## Lesson 0.2 - Where does data live while a program runs?
It stores in RAM

-> Program needs data 
-> Data must exists somewhere, while program runing
-> Locations in memory have addresses

What is a memory address?
A memory address is a number that identifies a location in memory where data can be accessed

Memory Address => Where
Data/Value => What

---

## Lession 0.3 - What is a variable? 
A variable is a name that refers to the memory address where data is stored. It allows us to access and manipulate the data stored in that memory location.

---

## Lession 0.4 - One value vs many values
When we have single value, we can directly store under one variable
price = 55

When we have 5. assume as below
price1 = 1
price2 = 2
price3 = 3
price4 = 4
price5 = 5

What if 100's, or 1000's, or millions?
price1= 1
...
price10000000 = 1000000
that's not the right way. there should be some ways.

So we need some other representation, assuming here is array or list in python
prices = [1,2,3,4... 1000000]

Now we used one super power. But power comes with responsibility. So if I need to delete one information means? if I need to search something means? if I need to find the max in that means? if I need to insert something inbetween means? there the problem happens. So we need some ways to handle those. 

---


---

## Need Clarity
1. How python variable knows the memory address. 
Example: a = 5
a is some address inside the RAM, and the data is stored as 5. But, how we know a is that address, where we have that information

